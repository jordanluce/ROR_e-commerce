<%= render partial: 'admin/shared/validation_errors', locals: {object: @size} %>
<%= form_for [:admin, @size] do |f| %>
	<p><%= f.text_field :name, placeholder: 'size' %></p>
	<p><%= f.submit 'Save Size' %></p>
<% end %>